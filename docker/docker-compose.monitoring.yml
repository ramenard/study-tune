services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    restart: unless-stopped
    volumes:
      - uptime_kuma_data:/app/data
    networks:
      - default
      - proxy

networks:
  proxy:
    external: true
    name: docker_default

volumes:
  uptime_kuma_data:
